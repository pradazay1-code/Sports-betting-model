"""Same-game correlation checks for parlays/slips (spec §5).

BUILD PHASE 4 — scaffold only (spec §9). Not yet implemented.

Block negatively correlated legs, flag positive stacks (size down),
max 2 legs per game per slip.
"""

from __future__ import annotations


def not_built() -> None:
    raise NotImplementedError(__doc__)
