"""Closing Line Value tracker — the true scoreboard (spec §0.2, §5).

BUILD PHASE 3 — scaffold only (spec §9). Not yet implemented.

Log open vs closing line for every pick (close = Pinnacle or de-vigged consensus),
grade picks, accumulate CLV by sport/prop/tier.
"""

from __future__ import annotations


def not_built() -> None:
    raise NotImplementedError(__doc__)
