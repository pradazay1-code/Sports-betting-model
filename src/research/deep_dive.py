"""Claude-driven research pass for EV > 6% picks (spec §4).

BUILD PHASE 5 — scaffold only (spec §9). Not yet implemented.

48h news sweep, verify the model's key assumption (find WHY the market disagrees),
kill-switch questions (lineup change? pitch count? minutes cap?), 3-5 sentence memo
with sources + timestamps. Model finds candidates; research validates them.
"""

from __future__ import annotations


def not_built() -> None:
    raise NotImplementedError(__doc__)
