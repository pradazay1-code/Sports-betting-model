[project]
name = "edge-engine"
version = "0.1.0"
description = "Autonomous sports betting research & prop analysis system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
