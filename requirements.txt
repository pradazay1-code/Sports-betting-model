# Core — needed for the odds feed, caching, and env loading.
requests>=2.31
python-dotenv>=1.0

# Analysis
pandas>=2.0
numpy>=1.24

# Scraping (UFC/BKFC/PFR/etc. have no APIs)
beautifulsoup4>=4.12
lxml>=5.0

# Per-sport data. Heavier; skip any you don't need for your sports.
nfl_data_py>=0.3.2
pybaseball>=2.2
nba_api>=1.4

# Dev
pytest>=8.0
