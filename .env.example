# Edge Engine — secrets live here (or in repo/environment secrets), never in YAML.
# Free key (500 requests/month): https://the-odds-api.com
ODDS_API_KEY=

# Phase 5: soccer stats (free tier): https://www.football-data.org
FOOTBALL_DATA_API_KEY=

# Optional override for the ledger location (defaults to data/db.sqlite)
# EDGE_DB_PATH=

# Phase 5: delivery
# DISCORD_WEBHOOK_URL=
# SMTP_URL=
